# 2. Check current status (optional but recommended)
git status

# 3. Stage ALL files (tracked and untracked)
git add .

# 4. Commit the staged files with a message
git commit -m "Initial upload or update of all project files"

# 5. Push the commit to the remote repository (main branch)
git push origin main